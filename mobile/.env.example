# Copy to .env and point at your backend.
# Use your Mac's LAN IP (not localhost) so a physical device can reach it:
#   ipconfig getifaddr en0
EXPO_PUBLIC_API_URL=http://192.168.x.x:8000/api

# Optional request timeout override (ms). Defaults to 20000.
# EXPO_PUBLIC_API_TIMEOUT_MS=20000
