[build]
builder = "NIXPACKS"
buildCommand = "pip install setuptools && pip install -r requirements.txt"

[deploy]
startCommand = "cd backend && python manage.py migrate --noinput && python manage.py migrate_schemas --schema=public --noinput && gunicorn config.wsgi --bind 0.0.0.0:$PORT"
