'use client';

import MessagingPage from '@/components/messaging/MessagingPage';

export default function AdminMessagesPage() {
    return (
        <MessagingPage emptyStateMessage="Choose a conversation to message staff, teachers, or parents." />
    );
}
